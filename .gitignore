node_modules/
dist/
backend/data/
backend/logs/
.env
frontend/.env
backend/.env
*.log
certs/
.compose-files
.claude/skills/dd-architecture-healthcheck/snapshots/
.DS_Store

# Claude Code's permission-allowlist files record approved shell commands
# verbatim, which can end up containing credentials/secrets typed during a
# session — never safe to version.
.claude/settings.json
.claude/settings.local.json
