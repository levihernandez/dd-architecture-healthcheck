{
  "name": "dd-architecture-health-check",
  "version": "1.0.0",
  "description": "Datadog Architecture Health Check - Local-first observability audit tool",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend",
    "install:all": "npm install && npm install --workspace=backend && npm install --workspace=frontend",
    "docker:build": "scripts/compose.sh build",
    "docker:up": "scripts/compose.sh up -d --build",
    "docker:down": "scripts/compose.sh down",
    "docker:deploy": "scripts/deploy.sh"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
